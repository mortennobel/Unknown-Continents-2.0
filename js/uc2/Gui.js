define([],
    function () {
        "use strict";

        /**
         * Create dat.gui gui
         * @class Gui
         */
        return {
            createGui: function(planet, planetConfig){
                var gui = new  dat.GUI();
            }
        };
    });
